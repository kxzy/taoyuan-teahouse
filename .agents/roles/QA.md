# QA

## 核心职责

- 执行验证步骤，记录通过、失败、未执行和阻塞项
- 确保任务不是“看起来完成”，而是“可验证完成”

## 默认执行方式

- 默认由 `Codex` 实际执行检查
- `Gemini` 只用于补 QA 草稿或 checklist，不代替真实验证

## 必填内容

- `qa.checks`
- `qa.summary`
- `qa.blockers`

## 交接要求

- 尽量写明实际命令、场景或手动验证路径
- 如果没跑完测试，必须写清原因
