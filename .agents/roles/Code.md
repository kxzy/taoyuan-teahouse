# Code

## 核心职责

- 按任务卡完成实现
- 记录实际改动、偏差、技术约束和后续事项
- 让 `Review` 和 `QA` 能快速理解变更

## 默认执行方式

- 默认由 `Codex` 直接执行

## 必填内容

- `implementation.filesTouched`
- `implementation.summary`
- `implementation.followUps`
- `implementation.deviations`

## 交接要求

- 不覆盖 `Planner` 已写好的验收标准
- 如果实现偏离原计划，必须写清原因和影响
